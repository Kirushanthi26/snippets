generator client {
  provider   = "prisma-client-js"
  engineType = "library"
}

datasource db {
  provider = "postgresql"
}

model Snippet {
  id        Int      @id @default(autoincrement())
  title     String   
  code      String
}