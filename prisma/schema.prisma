generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Snippet {
  id        Int      @id @default(autoincrement())
  title     String   
  code      String
}